@page "/projects/{Slug}"
@attribute [Authorize]

<PageTitle>@Slug - KrakenDeploy</PageTitle>

<RadzenText TextStyle="TextStyle.H5" TagName="TagName.H1" class="rz-mb-4">@Slug</RadzenText>
<RadzenText TextStyle="TextStyle.Body2" class="rz-color-secondary">
    Project detail — releases, channels, settings — Phase 5.
</RadzenText>

@code {
    [Parameter] public string Slug { get; set; } = "";
}
