// The canonical StepHandlerContext now lives in KrakenDeploy.Contracts.Steps.
// See IStepHandler.cs in this folder for the global using re-export that
// keeps existing in-DI handlers building without churning every file.
